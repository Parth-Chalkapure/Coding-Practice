#include<bits/stdc++.h>
using namespace std;
// void -> which does not return anything

void printName(string x) {
    cout << "Hey " << x << endl;
}

int main() {
    string name;
    cin >> name;
    printName(name);

    string name2;
    cin >> name2;
    printName(name2);
    return 0;
}
